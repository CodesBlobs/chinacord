# Replace these values before production use.
realm=dropzone.example.com
server-name=dropzone-turn
fingerprint
lt-cred-mech
user=dropzone:replace-this-password

# Replace with your public IP or DNS name.
external-ip=127.0.0.1

listening-port=3478
tls-listening-port=5349

min-port=49160
max-port=49200

no-multicast-peers
no-cli
no-loopback-peers
stale-nonce=600

log-file=stdout
simple-log
